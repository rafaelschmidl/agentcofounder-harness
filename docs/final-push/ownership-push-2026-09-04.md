# Local final technical push — 4 September 2026

Started 22:14 Stockholm. Rafael owns publication and submission; this execution makes local changes and commits only. The outer directory is unversioned. Starting harness `d77811f`, evaluator `088c218`, both on `final-push/2026-09-04`, clean. Existing candidate documents are historical evidence, not current qualification.

## Decisions and work in progress

- Root integrates and exclusively coordinates paid runs. Independent bounded agents own generated UX, accounting/evaluator calibration, and runtime compatibility. No other agent may make model calls.
- Preserve Jones's integrated design-family work and Rafael's runtime/compiler history. Keep compiled flat-collection behavior with free generated product UI and custom fallback; substantial redesign this late needs stronger evidence than the observed working cases provide.
- Improve reachable creation/editing and mobile record/action composition in reusable guidance and styles. Validate fresh generated products from one committed candidate, without manual app repair.
- Accept exact parenthesized live counts in independent evaluation. A visible `Lent out (1)` is a valid count, while wrong values, fractions, decimals and unrelated totals must remain rejected. Preserve original reports and calibrate with a broken-counter control.
- Harden offline dependency preparation and document the actual Linux ARM64 proof boundary. Docker daemon availability is unresolved; source configuration alone is not a successful container run.
- Run the public Book and a contrasting small stateful brief on the selected clean SHA. All existing A/B prompts have already been generated; their reuse cannot be called a new unseen assessment. Preserve the first B result as historical transfer evidence.

## Budget boundary

New authorization: at most **€8.15 additional**, from a user-reported €10.15 balance, with **€2 protected**. The older evaluator and provider ledgers are not new authority. Before any paid run: establish no active model processes, settle the ended commerce reservation from complete request evidence, retain all unknown request ceilings, and record a timestamped execution baseline. Keep the existing provider guard if its remaining allowance is stricter than the new authorization. Per-run Pi cost and W remain separate from EUR charges computed from raw provider usage.

Final selection, commands, screenshots, run totals, reservations and limitations will be recorded here after qualification.
