# Product interface and journey builder

The compiler has implemented the complete flat collection domain and persistence. Generate exactly the three AGENT-owned files in BuildPlan: `src/product/App.tsx`, `src/product/styles.css`, and `src/product/product.test.tsx`. Stop after those three distinct complete files are successfully written. Do not emit prose or explanations; begin with write. Domain and system files are protected. All concrete imports, data fields, defaults and action IDs appear in the supplied compiled `src/product/domain.ts` and system API.

In App import `definition`, `useProductCollection`, and optionally `ProductEditor` from `./domain`. Call `useProductCollection()` inside App. It owns records, canonical saved state, editor, validation, guarded actions, recovery and notices. Render product-specific rows/board/list, filters, counts, and controls from that controller. `controller.startCreate()`, `startEdit(record)`, `remove(record)`, `act(record, actionId)`, `cancel()` and `submit(values)` are the actual operations. `definition.actions` has labels/available(record) and input fields; `definition.fields` describes editable fields; every record property is a string. Use the exact declared keys. `recordKeys` maps canonical entity field IDs to runtime properties; the generated identifier is always `record.id`, even when its canonical field ID differs. Render controller.notice visibly as error (`role=alert`) or success (`role=status`). Show only product-required actions; controller capabilities do not expand product scope.

Use `<ProductEditor controller={controller} className="..." fieldsClassName="..." />` at a purposeful place in the layout, or build custom native forms calling controller.submit if the task warrants them. ProductEditor already handles Enter, invalid drafts, errors, focus, switching records and hidden-state preservation. UI-only state such as filters, expansion or view choice remains yours. Do not recreate canonical collection state, localStorage persistence, repositories, workflow logic, or UUIDs. There is no Store export. Do not mount React: export default App and let the linked main.tsx own createRoot. App must render safely as `<App />`.

Use product.experience as initial art direction. Give the product a concise believable name; match composition, typography, density and palette to its real work. Create a useful first screen with its collection/workflow and obvious next action. No landing hero, invented metrics, implementation copy or decorative feature cards. Product CSS takes precedence over the supplied foundation; establish coherent page, surface, text, primary-action and on-accent colors together. Choose readable rows, grouped workflow columns, a shelf, or another task-appropriate composition. All page JSX and CSS are yours. Helpers do not prescribe a layout. Use the supplied Foundation design API, native controls, system fonts and CSS; no network assets or new packages. Keep visible focus, readable contrast, 44px touch targets, distinct empty/no-results states and destructive controls. At 390px adapt the working area and wrap related controls efficiently.

Implement every IMPLEMENT requirement and exclude PROPOSE/EXCLUDE scope. Keep filters/counts derived from the actual current records. Scope repeated controls with unique accessible names or a named form/region. Preserve nondefault values, edits, current workflow state and refresh persistence. Error feedback must describe failed actions and preserve the current draft/state.

Write real Vitest/Testing Library journeys in product.test.tsx for every verification obligation. Tag exact identifiers in executed test titles: `[journey_id]`. Use user-visible actions and assertions, plus domain tests through `prepareCollectionAction(definition, actionId, record, input)` from the supplied collection-controller API for guards/invariants. Assert the intended requirement, not merely the implementation's returned value. Domain/compiler errors are failures; do not weaken tests or implement alternate semantics in App. Check nondefault fields, counts and connected resulting state. Use `within` for repeated labels/text, name forms before querying role=form, assert alert text within its region, and use real localStorage across unmount/remount. Storage-failure tests use `vi.spyOn(Storage.prototype, "setItem")`. BLOCK-owned setup cleans DOM/storage and restores spies between tests.

Write only the three required AGENT files. Deterministic checks and the existing repair loop provide failure evidence.
